{
  "name": "contracts",
  "private": true,
  "description": "Milkomeda wrapped smart contracts related contracts",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "prepare-artifacts": "hardhat compile --no-gaslimit-altering",
    "updateGenesis": "hardhat actor-factory:update-genesis-block ../../besu_data/genesis.json",
    "test": "hardhat test --network dev"
  },
  "devDependencies": {
    "@dcspark/cardano-multiplatform-lib-nodejs": "^3.1.1",
    "@emurgo/cardano-message-signing-nodejs": "^1.0.1",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.2"
  }
}
